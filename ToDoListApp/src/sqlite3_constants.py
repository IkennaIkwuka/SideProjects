class Constants:
    # Constraints
    PRIMARY_KEY = "PRIMARY KEY"
    AUTOINCREMENT = "AUTOINCREMENT"
    NOT_NULL = "NOT NULL"
    UNIQUE = "UNIQUE"
    FOREIGN_KEY = "FOREIGN KEY"
    REFERENCES = "REFERENCES"
    CHECK = "CHECK"
    DEFAULT = "DEFAULT"

    # Datatypes
    INTEGER = "INTEGER"
    TEXT = "TEXT"
    REAL = "REAL"
    BLOB = "BLOB"
    NULL = "NULL"


sql = Constants()
