# Mortgage Calculator

Command-line mortgage repayment calculator. Takes a loan principal, monthly interest rate, and loan term (in years), and computes the fixed monthly payment using the standard amortization formula.

## Run

```bash
javac -d bin src/main/Main.java
java -cp bin main.Main
```

## Structure

```
src/main/Main.java   # entry point and all logic
```
